{
  "watch": ["./src/**/*.ts", ".env"],
  "ext": "ts,js,.env",
  "verbose": false,
  "exec": "node -r ts-node/register --inspect ./src/index.ts",
  "execMap": {
    "ts": "node --require ts-node/register"
  }
}
